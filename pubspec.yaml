name: momnah_portfolio
description: "A new Flutter project."
publish_to: 'none' # Prevents accidental publishing

version: 1.0.0+1

environment:
  sdk: '>=2.14.0 <3.0.0' # Adjusted to fit real Dart versions and your local Dart version

dependencies:
  flutter:
    sdk: flutter
  confetti: ^0.6.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - images/arrow.jpg
